{
  "builders": [
    {
      "ami_block_device_mappings": [
        {
          "delete_on_termination": true,
          "device_name": "xvda",
          "encrypted": true,
          "volume_size": 8,
          "volume_type": "gp2"
        }
      ],
      "ami_name": "freeipa-server-hvm-{{timestamp}}-x86_64-ebs",
      "ami_regions": "{{user `deploy_regions`}}",
      "instance_type": "t3.micro",
      "launch_block_device_mappings": [
        {
          "delete_on_termination": true,
          "device_name": "xvda",
          "encrypted": true,
          "volume_size": 8,
          "volume_type": "gp2"
        }
      ],
      "region": "{{user `build_region`}}",
      "source_ami_filter": {
        "filters": {
          "name": "Fedora-Cloud-Base-29-*x86_64-hvm-*-gp2-*",
          "root-device-type": "ebs",
          "virtualization-type": "hvm"
        },
        "most_recent": true,
        "owners": [
          "125523088429"
        ]
      },
      "ssh_username": "fedora",
      "tags": {
        "Application": "FreeIPA server",
        "Base_AMI_Name": "{{ .SourceAMIName }}",
        "OS_Version": "Fedora 29",
        "Pre_Release": "{{user `pre_release`}}",
        "Release": "{{user `image_version`}}",
        "Team": "CISA - Development",
        "Travis_Job_Web_URL": "{{user `travis_job_web_url`}}"
      },
      "type": "amazon-ebs"
    }
  ],
  "provisioners": [
    {
      "groups": [
        "aws",
        "freeipa"
      ],
      "playbook_file": "src/playbook.yml",
      "type": "ansible"
    }
  ],
  "sensitive-variables": [],
  "variables": {
    "build_region": "{{env `PACKER_BUILD_REGION`}}",
    "deploy_regions": "{{env `PACKER_DEPLOY_REGIONS`}}",
    "image_version": "{{env `PACKER_IMAGE_VERSION`}}",
    "pre_release": "{{env `PACKER_PRE_RELEASE`}}",
    "travis_job_web_url": "{{env `TRAVIS_JOB_WEB_URL`}}"
  }
}
